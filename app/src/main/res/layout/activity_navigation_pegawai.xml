<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <fragment
        android:id="@+id/nav_host_fragment_pegawai"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:defaultNavHost="true"
        app:layout_constraintBottom_toTopOf="@+id/nav_view"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:navGraph="@navigation/ap_navigation_pegawai" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/nav_view_pegawai"
        android:layout_width="0dp"
        android:layout_height="70dp"
        android:background="@color/primaryDark"
        android:clipChildren="false"
        app:itemRippleColor="@color/blue50"
        app:backgroundTint="@color/primaryDark"
        app:itemActiveIndicatorStyle="@style/NavigasiAktif"
        app:itemTextColor="@color/white"
        app:itemIconTint="@color/white"
        app:itemPaddingBottom="11dp"
        app:itemPaddingTop="9dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:menu="@menu/menu_navigasi_pegawai" />


</androidx.constraintlayout.widget.ConstraintLayout>